# Git 仓库重置和推送操作指南

## 操作说明

由于PowerShell遇到编码问题，我已经为你创建了一个批处理脚本来完成Git仓库的重置和推送操作。

## 使用方法

### 方法一：使用批处理脚本（推荐）

1. 在项目根目录找到文件：`完全重置并推送.bat`
2. 双击运行该文件
3. 按照提示操作，确认后继续
4. 等待脚本自动完成所有操作

### 方法二：手动执行命令

如果你想手动执行，请按以下步骤操作：

```bash
# 1. 进入项目目录
cd "d:\Lan_Company\AI-TYKJ\08-AI价格智能体"

# 2. 删除 .git 文件夹（清空历史）
rd /s /q .git

# 3. 初始化新仓库
git init

# 4. 设置默认分支为 main
git branch -M main

# 5. 添加所有文件
git add .

# 6. 创建初始提交
git commit -m "初始提交: AI价格智能体项目 - UI风格统一完成"

# 7. 添加新的远程仓库
git remote add origin https://github.com/DPXJ/TYKJ_AI-price.git

# 8. 强制推送到远程仓库
git push -u origin main --force
```

## 脚本功能说明

`完全重置并推送.bat` 脚本将执行以下操作：

1. ✅ **删除 .git 文件夹** - 清空所有历史记录
2. ✅ **初始化新仓库** - 创建全新的Git仓库
3. ✅ **设置主分支** - 将默认分支设置为 main
4. ✅ **添加文件** - 将所有项目文件添加到暂存区
5. ✅ **创建提交** - 创建新的初始提交
6. ✅ **配置远程** - 添加新的GitHub仓库地址
7. ✅ **验证配置** - 显示远程仓库配置信息
8. ✅ **推送代码** - 强制推送到新仓库

## 新仓库信息

- **仓库地址**: https://github.com/DPXJ/TYKJ_AI-price.git
- **默认分支**: main
- **提交信息**: "初始提交: AI价格智能体项目 - UI风格统一完成"

## 注意事项

⚠️ **重要提示**:

1. **此操作将完全清空Git历史记录** - 所有旧的提交记录将被删除
2. **强制推送会覆盖远程仓库** - 如果远程仓库有内容，将被完全替换
3. **请确保新仓库地址正确** - 推送前请确认 https://github.com/DPXJ/TYKJ_AI-price.git 是正确的目标仓库
4. **备份重要数据** - 在执行前，建议备份项目文件

## 执行后验证

脚本执行完成后，你可以通过以下方式验证：

1. 访问 https://github.com/DPXJ/TYKJ_AI-price 查看是否推送成功
2. 检查仓库中是否包含所有项目文件
3. 查看提交历史是否为全新的初始提交

## 遇到问题？

### 问题1: 推送时需要登录

如果推送时提示需要登录，你可以：
- 使用 GitHub Desktop 应用
- 使用 `gh auth login` 命令（需要安装 GitHub CLI）
- 配置 SSH 密钥

### 问题2: 推送被拒绝

如果推送被拒绝，确认：
- 你有该仓库的写入权限
- 仓库地址正确
- 使用了 `--force` 参数（脚本已包含）

### 问题3: 无法删除 .git 文件夹

如果无法删除 .git 文件夹：
- 关闭所有Git相关程序（如 GitHub Desktop、VS Code等）
- 以管理员身份运行脚本
- 手动删除 .git 文件夹

## 下一步

推送成功后，你可以：

1. 在GitHub上查看项目
2. 配置仓库设置（如README、.gitignore等）
3. 邀请团队成员
4. 设置GitHub Pages（如果需要）

---

**创建时间**: 2026年1月10日
**脚本版本**: 1.0
