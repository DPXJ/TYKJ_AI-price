# 价格智能体对话框定位修复说明

## 修复时间
2026年1月12日

## 问题描述
在价格智能体对话页面中，底部输入框超出了手机壳的边界，显示在手机屏幕外部。

## 问题原因
1. `.price-chat-input` 使用了 `position: fixed`，导致输入框相对于浏览器视窗定位，而不是相对于手机壳
2. `.price-chat-page` 使用了 `height: 100vh`，撑满了整个视窗高度，而不是适应手机壳的高度

## 修复方案

### 1. 修改输入框定位方式
**文件**: `styles/main.css` (第 13815-13827 行)

```css
.price-chat-input {
    position: absolute;  /* 原为 fixed，改为 absolute */
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 16px;
    background: white;
    border-top: 1px solid #e0e0e0;
    box-shadow: 0 -2px 8px rgba(0,0,0,0.05);
}
```

### 2. 修改页面容器高度
**文件**: `styles/main.css` (第 13751-13756 行)

```css
.price-chat-page {
    display: flex;
    flex-direction: column;
    height: 100%;         /* 原为 100vh，改为 100% */
    background: #f8f9fa;
    position: relative;   /* 新增，为绝对定位的子元素提供定位上下文 */
}
```

## 修复效果
- ✅ 输入框现在正确显示在手机壳的底部
- ✅ 输入框不再超出手机屏幕边界
- ✅ 对话页面完全适配手机壳的尺寸
- ✅ 保持了原有的功能和交互效果

## 技术说明

### position 属性区别
- `position: fixed` - 相对于浏览器视窗定位，滚动时保持位置不变
- `position: absolute` - 相对于最近的已定位祖先元素定位

### 高度设置区别
- `height: 100vh` - 视窗高度的 100%（整个浏览器窗口）
- `height: 100%` - 父容器高度的 100%（手机壳内部区域）

## 相关文件
- `styles/main.css` - 主样式文件
- `index.html` - HTML 结构
- `scripts/main.js` - JavaScript 交互逻辑

## 测试建议
1. 打开价格智能体对话页面
2. 检查输入框是否正确显示在手机壳底部
3. 输入文字并发送消息，确认交互正常
4. 查看多条消息时，确认页面滚动正常
5. 测试不同屏幕尺寸下的显示效果
